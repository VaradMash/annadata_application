<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.annadata">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Annadata">
        <activity android:name=".ViewDonationDetails"></activity>
        <activity android:name=".ViewDonations" />
        <activity android:name=".EditRequest" />
        <activity android:name=".NewRequestActivity" />
        <activity android:name=".RequestActivity" />
        <activity android:name=".EditDonation" />
        <activity
            android:name=".NewDonationActivity"
            android:noHistory="true" />
        <activity
            android:name=".DonationActivity"
            android:noHistory="true" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".UserProfile" />
        <activity
            android:name=".MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegistrationActivity"
            android:noHistory="true" />
        <activity
            android:name=".LoginActivity"
            android:noHistory="true" />
    </application>

</manifest>